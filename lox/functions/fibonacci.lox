fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = clock();
println(fib(35));

var end = clock() - start;
// May take more than 1 minute
println("Time in seconds: " + end);